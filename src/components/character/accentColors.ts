import type { AccentColor } from '@/data/types';

export const accentHex: Record<AccentColor, string> = {
  mustard: 'var(--color-mustard)',
  sky: 'var(--color-sky)',
  blush: 'var(--color-blush)',
  sage: 'var(--color-sage)',
};

export const accentSoftHex: Record<AccentColor, string> = {
  mustard: 'var(--color-mustard-soft)',
  sky: 'var(--color-sky-soft)',
  blush: 'var(--color-blush-soft)',
  sage: 'var(--color-sage-soft)',
};
